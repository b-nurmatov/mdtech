---
const { name, ariaLabel, class: extraClasses = "" } = Astro.props;
---

<svg
    width="24"
    height="24"
    fill="none"
    stroke="currentColor"
    stroke-width="0"
    stroke-linecap="round"
    stroke-linejoin="round"
    class={extraClasses}
    aria-hidden={ariaLabel ? "false" : "true"}
    aria-label={ariaLabel ? ariaLabel : ""}
>
    <use type="image/svg+xml" href=`/sprite.svg#${name}`></use>
</svg>
