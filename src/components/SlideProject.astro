---
import CardImageBlock from "./CardImageBlock.astro";

import pages from "../data/pages.json";

const { title, tags, image, href, tagsHref } = Astro.props;
---

<aside>
    <CardImageBlock
        tags={tags}
        tagsSeparator="comma"
        tagsDarker={true}
        tagsHref={tagsHref}
        image={image}
        infoBlock={true}
        overlay={true}
        brightnessClass="brightness-50"
        rounded={true}
        class="pb-2"
        ><h3 class="text-base/[120%] font-bold text-white">
            <a href={href} set:html={title}></a>
        </h3></CardImageBlock
    >
</aside>
